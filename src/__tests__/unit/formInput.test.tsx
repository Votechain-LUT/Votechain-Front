import React from "react";

describe("Form input", () => {
  it("changes input value properly", () => {
    expect(2).toBe(2);
    // const onChangeMock = jest.fn();
    // const event = {
    //   preventDefault() {},
    //   target:{ value: "test123" }
    // };
    // const body: RenderResult = render(<FormInput type={"text"} value={"testValue"} label={"test"} placeholder={"testPlaceholder"} onChange={onChangeMock}/>);
    // const input = body.getByPlaceholderText("testPlaceholder");
    // fireEvent.change(input, event);
    // expect(onChangeMock).toBeCalledWith("test123");
  });
});
